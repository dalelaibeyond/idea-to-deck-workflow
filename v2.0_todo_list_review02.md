# V2.0 Todo List Review 02

## 审阅结论

### 1. “Golden 基准” 的定位应当是“参考对照基线”，而不是“最终审批标准”

在当前 V2.0 方案中，golden 的作用应理解为：

- 旧版输出被视为一个历史可接受的参考样本
- 新版重构时用于回归验证，确保没有无意造成视觉或结构退化
- 不是要求新输出必须强绑定旧版 PPTX / HTML 的逐字节一致

因此，golden 的价值在于“稳定性与可验证性”，而不是“锁死历史设计结果”。

如果最终视觉由人决定，那么 golden 只能作为参考，不应成为最终的唯一真相来源。

---

### 2. 人决定视觉，不等于放弃工程化校验

用户需求是：

- 最终的 PPTX / HTML 效果由人决定
- 不依赖历史的 presentation.pptx / presentation.html 作为唯一标准
- 但仍然需要一个可控、可复核、可稳定重构的生成流程

这与 V2.0 的工程目标并不冲突。V2.0 的重点是：

- 让渲染逻辑可复用
- 让输出可验证
- 让结构参数、布局标签、设计元数据可审查
- 让内容与视觉不再靠手工复制、手工同步

换句话说：

- 人负责“选择什么是好看的”
- 工程负责“保证稳定生成和可回归”

这两者是兼容的。

---

### 3. “历史输出对比”应采用程序化校验，而不是让 AI 逐页逐张审阅

使用 Git 历史里的旧产物做比对是合理的，但应避免做成下面这种高成本流程：

- 把旧 PPTX / HTML 全量喂给 AI
- 让模型逐页逐张分析视觉差异
- 再一大段地输出审阅结论

这种方式会带来明显的 token 成本，而且并不必要。

更合理的方式是：

- 使用脚本提取关键信息：slide 数、标题、文本块、Layout Tag、主题变量
- 自动比较新旧输出的结构差异
- 只在关键差异处让人审阅
- 人只确认“是否接受这项差异”，而不是对整套历史产物做重复性阅读

这是更经济、更稳妥的方案。

---

### 4. Golden 需要从“历史快照”升级为“批准基线”或“设计参考口径”

如果项目要兼容“人决定最终视觉”，那么最稳妥的表述应为：

- golden = approved baseline / approved visual reference
- 而不是单纯的 git 历史 blob

这样做的好处是：

- 保留回归分析能力
- 不把新轮次设计绑死在旧产物上
- 允许在设计评审后更新新的基线

也就是说，golden 可以是“当前接受的视觉标准”，而不是“过去某次提交的最终真相”。

---

## 结论性建议

### 建议保留 V2.0 的核心目标，但修正 golden 的表达方式

建议把以下语义明确下来：

1. V2.0 目标：
   - 让 deck 生成更稳定、可复用、可验证
   - 让设计和内容的同步更可控
   - 让最终视觉由人审批，而不是依赖手工复制和隐性漂移

2. golden 的定义：
   - 作为参考对照基线，而不是唯一最终标准
   - 若需使用历史产物，可仅作回归对照，不应成为强制复制关系

3. 评审方式：
   - 程序做差异检测
   - 人看关键页面与关键差异
   - 仅在必要时重置或更新基线

---

## 实施提醒

### 需要特别注意的几点

- 不要让 golden 变成“必须复刻历史 PPTX/HTML”的条条框框
- 不要让 AI 在全量历史产物上做重复性逐页审阅
- 让脚本负责结构和一致性检测，让人负责视觉选择
- 若要保留历史对比，优先做“文本/布局/颜色/slide-count 自动比对”，而非“视觉直观逐页推演”

---

## 最终判断

V2.0 与“人决定最终视觉”的需求并不冲突。

冲突的根源不是 V2.0 本身，而是把 golden 理解成了“必须跟历史输出完全一致”。

更合理的理解是：

- golden 是回归参考
- 人是最终设计决策者
- 自动化是校验工具

这三者组合，才符合这个项目真正的工程目标。
