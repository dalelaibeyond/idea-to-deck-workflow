# Review 01 — V2.0 Todo List 启动前评估

> **评审对象:** `v2.0-todo-list.md`（已并入 review-7th P0 修订版）
> **评审日期:** 2026-09-16
> **评审方式:** 仓库现状事实核查 + git 历史 blob 溯源 + 渲染确定性现场实证
> **评审结论:** ✅ **GO** — 计划与仓库现状高度吻合，未发现事实错误；建议补入 3 项开工前置事项（见 §4）后批准 S1

---

## 1. 评审范围与方法

本次评审不限于文档通读，对 todo 中的关键事实断言逐条做了实证核查：

- 仓库工作区与 gitignore 状态检查
- golden 基准 blob（`ea71cab` / `c46fc87`）可提取性验证
- 历史版本关键工件的结构与规模测量（build_deck.py / design.md / deck-content.md / marp_deck.md）
- 渲染确定性双盲探测（同输入重跑 2 次 + 重跑 vs golden，part 级内容哈希对比，见 §3）

---

## 2. 事实核查：todo 断言 vs 仓库现状

| # | todo 断言 | 核查结果 | 状态 |
| :- | :--- | :--- | :--- |
| 1 | Golden 可从 git blob 提取（PPTX=`ea71cab`，HTML=`c46fc87`） | 两 blob 均存在且可提取：50,764 B / 186,322 B | ✅ |
| 2 | v1.0 tag 不含产物，勿依赖 `_samples/` 副本 | v1.0 → `1593d8a`（reset 之后），该点 06-outputs 仅剩 `.gitkeep`；`_samples/` 确在 gitignore 区 | ✅ |
| 3 | `build_deck.py` 约 350 行，几何内联在函数体 | 346 行；结构为 `THEME` dict + 6 个 `render_*` 函数（几何内联）+ `main()` 内联数据——与 B2（几何抽出）/ B3a（数据层抽离）重构方案完全对位 | ✅ |
| 4 | 6 个 archetype 命名与词汇表统一 | design.md registry 6 个条目 == build_deck.py 6 个 render 函数，命名一一对应 | ✅ |
| 5 | A2 双层化是「扩展」而非重建 | design.md @ `48d238d` 已有含 theme hex / 字体的 frontmatter（13 行）；缺几何寄存器、Layout Tag 词表、slide→archetype 映射的数据化——均为增量扩展 | ✅ |
| 6 | A4 有可解析基线 | deck-content.md 已有 frontmatter（`total_slides` / `language`）+ 每页 `## Slide N` / `* Layout Tag:` / `* Voiceover Script:`（单行长引文）结构 | ✅ |
| 7 | 双语料：sample02 = 英文 deck | frontmatter 明示 `language: "en-US"` | ✅ |
| 8 | 工具链就绪 | python-pptx 1.0.2 + Marp CLI 4.5.1 均已安装 | ✅ |
| 9 | `10-tools/` 命名可行、将被 git 跟踪 | 不在 .gitignore 内；`__pycache__/` 已忽略，适合 Python 包 | ✅ |
| 10 | 双语料：sample01 = 中文 deck | **未验证**（见 §4-2）。按 AGENTS.md 排除规则未读 `_samples/` 内容；仅从文件名可见 sample01 缺 marp_deck.md / presentation.html（只有 PPTX 路径，对 A4 解析验收无碍） | ⚠️ |

**结论：无事实错误。** 唯一未闭环项是 sample01 的语言属性（需人工 1 分钟确认，不阻塞 S1/S2）。

---

## 3. 新增实证：渲染确定性探测（本次评审跑出，todo 未覆盖）

在 /tmp 隔离环境用 `ea71cab` 的 build_deck.py 做了双盲探测（命令见附录 A）：

1. **同输入重跑 2 次**：文件级 SHA 不同（zip entry mtime 所致，符合 review-7th §1.1 结论）；但**解包后所有 XML part 内容哈希 100% 一致**——B2 的核心前提（内容级确定性）在当前引擎上**今天已成立**，重构后的确定性验证不存在原理性风险。
2. **重跑 vs golden**：用当前工具链（Python 3.14 + python-pptx 1.0.2）从 git blob 重建，产物与 golden `ea71cab` **全部 part 逐字节一致**——证明：
   - S1 的 golden 对比验收程序现在即可执行；
   - 自 ea71cab 以来的工具链版本漂移未影响输出内容。
3. **附带发现**：core.xml 无墙钟写入问题（V1.0 脚本未设置 core properties）。B2 验收只需忽略 zip 元数据，**无需排除任何 XML part**——验收程序可比 todo 预期更简单、更严格（全 part 直接比对）。

---

## 4. 开工前建议补入的 3 项（均不改变阶段划分，只补细节）

### 4-1. S1 增加 Step-0「工作集恢复」

当前 `06-outputs/` 为空，B3a 所述「现 build_deck.py」只存在于 git 历史。S1 第一步应显式恢复工作集：

- `git show ea71cab:06-outputs/{build_deck.py,deck-content.md,design.md,info.md,outline.md}`
- `git show c46fc87:06-outputs/marp_deck.md`

这一步本身就是 golden 流程的冒烟测试（恢复 → 重跑 → §3-2 的 part 级比对），建议单独 commit 作为 S1 基线锚点。

### 4-2. S3 增加「测试语料落位」+ sample01 语言确认

A4 双语料验收依赖 `_samples/`，但该目录是 pipeline-blind + gitignored——渲染引擎测试永久依赖它是一个结构性矛盾（测试输入处于 agent 禁读区且不受版本控制）。建议：

- S3 开工时将两份语料复制为 `10-tools/render/tests/fixtures/` 下的受控、受跟踪文件；
- 顺带人工确认 sample01 确为中文 deck（评审受排除规则所限未读其内容；另注意归档 commit `1593d8a` 的措辞 "archive ICS AI Agent sample01 deck to _samples/sample02" 中 sample 编号存在语义歧义，以实际文件内容为准）。

### 4-3. 验收对比脚本工具化

本次探测所用 part 级哈希对比（约 10 行 shell/python）建议作为 S1 交付物落为 `10-tools/render/verify.py`，一举三得：

- B2 确定性验收工具（重跑一致性）；
- S1 golden 等价性验收工具（重跑 vs `ea71cab` blob）；
- C2 post-build 校验的工具种子。

同时需为 HTML 侧定义对比规则：Marp 输出含 CLI 版本等环境信息，内容级比对应限定 `<section>` 文本 + 主题 CSS 块（PPTX 侧已实证可全 part 直接比对，无需白名单）。

---

## 5. 规格级修订建议（不动结论，补强 A4）

todo 风险 §三 对 A4 的预判方向正确（引号/冒号/换行定界），但实证发现**真正的解析难点不止定界**：

1. **On-Slide Content 字段形状随 layout 变化**：现存语料中 6 种 layout 各有不同字段结构（`Metric N + Label` / `Left Anchor + Right Details` / cards / quadrants / steps……）。A4 规格除安全定界外，必须**为每个 archetype 定义字段 schema**，否则解析器无法机械还原嵌套结构。
2. **列表符混用**：现存 deck-content.md 同时使用 `-` 与 `*` 列表符。A4 规格需钉死单一约定（或解析器显式归一化）。
3. 测试语料充分性无虞：sample02 单 deck 已覆盖全部 6 种字段形状 + 全部 6 个 archetype，A4 双语料验收设计成立。

---

## 6. 无需修改的确认项

- **S1→S2→S3→S4 全量按序**：S1 契约边界（数据源 = 独立 `slides_data.json`，不依赖 S2 frontmatter / S3 解析）与现状兼容，无越界风险；review-7th 否决「仅 S1+S3」的理由经核查成立。
- **回滚策略**（每阶段独立 commit）：与 git 现状兼容，且 §4-1 的 Step-0 单独 commit 会使 S1 基线锚点更干净。
- **三项已裁决决策**（`10-tools/` 命名 / 实施顺序 / golden 走 git blob）：均与仓库事实一致，无遗留。
- **B3a 薄壳化目标**：346 → ~40-60 行的缩减空间真实存在（数据层与 6 个 render 函数占绝对主体）。

---

## 附录 A：确定性探测复现命令

```bash
# 准备隔离环境
rm -rf /tmp/v2probe && mkdir -p /tmp/v2probe/06-outputs && cd /tmp/v2probe
git -C <repo> show ea71cab:06-outputs/build_deck.py > build_deck.py

# 同输入重跑 2 次
python3 build_deck.py && cp 06-outputs/presentation.pptx run1.pptx
sleep 1 && python3 build_deck.py && cp 06-outputs/presentation.pptx run2.pptx
shasum run1.pptx run2.pptx   # 预期：不同（zip mtime）

# part 级内容哈希对比（run1 vs run2；再对 golden 重复一次）
for d in x1 x2; do mkdir -p $d; done
ditto -x -k run1.pptx x1/ && ditto -x -k run2.pptx x2/
diff <(cd x1 && find . -type f | sort | while read f; do shasum "$f"; done) \
     <(cd x2 && find . -type f | sort | while read f; do shasum "$f"; done)
# 预期：无输出（全部 part 一致）

# golden 对比
git -C <repo> show ea71cab:06-outputs/presentation.pptx > golden.pptx
mkdir xg && ditto -x -k golden.pptx xg/
# 同上 diff xg vs x1 → 预期无输出
```

实测结果（2026-09-16，Python 3.14 / python-pptx 1.0.2 / macOS darwin 25.5.0 arm64）：

- run1 vs run2：文件 SHA 不同，全部 XML part 一致 ✅
- rebuild vs golden（ea71cab）：全部 XML part 逐字节一致 ✅
