# V2.0 Todo List — Idea-to-Deck Pipeline

> **Status:** 已并入 review-01/02（review-8th）修订意见，S1 执行准备就绪
> **Created:** 2026-09-16
> **Based on:** V1.0 双路径渲染实操 + 6 轮架构讨论 + review-7th 评审（P0 修订）+ review-01/02 必要性核实与修订

---

## 背景与动机

V1.0 已跑通 5-phase 管线（PPTX + HTML 双路径输出）。V2.0 针对实操中暴露的结构性问题：**内容多处复制漂移**、**渲染规范层不确定性**、**渲染引擎不可复用**、**对齐性缺乏机械化校验**。

---

## 一、改进项目清单

### A. 数据与规范层（消除漂移根源）

- [ ] **A1. 内容单一源化**
  - `deck-content.md` 成为唯一文案源；`build_deck.py` / `marp_deck.md` 禁止内联文案
  - 渲染时从 `deck-content.md` 读取/解析，而不是复制粘贴
  - 验收：修改 deck-content.md 某页文案后重跑 Phase 5，两端输出同步更新，无人工二次编辑

- [ ] **A2. design.md 双层化**
  - frontmatter 增加**机器可读数据块**：theme hex、字号、几何寄存器、Layout Tag 词表、slide→archetype 映射
  - 正文保留**人面叙事**（设计意图、palette focus 理由），供 Phase 4 HITL 审阅
  - 验收：schema 校验可直接对 frontmatter 数据机械化执行；人审阅只看正文

- [ ] **A3. Layout Tag 单一权威**
  - `deck-content.md` 的 Layout Tag → design.md 映射 → 渲染，由**派生**实现，消除三处手工同步
  - 预检改为自动化一致性断言（而非人工比对）

- [ ] **A4. deck-content 解析规格（review-7th P0-3 新增）**
  - 定义 `deck-content.md` 的**安全定界规则**：action title / Layout Tag / on-slide 字段 / voiceover script / delivery mindset 逐页可机械解析
  - 口播稿雷区：中文引号「」/ 英文引号 / 冒号 / 换行 / 破折号避免误切分——建议 **YAML 块标定界**（`<!-- voiceover -->` 或 frontmatter 化）优先于散文式内联
  - **per-archetype on-slide 字段 schema（review-01 §5-1 补强）**：每种 archetype 必须钉死其字段键与嵌套形状（实证：6 个 render 函数签名各异——`HERO_CENTER`(takeaway/meta) vs `CARD_ROW_3`(cards) vs `GRID_2X2`(quadrants) vs `PROCESS_TIMELINE`(steps)……），解析器按 schema 机械还原嵌套结构，而非仅靠启发式定界
  - **列表符约定文档化（review-01 §5-2 修正）**：实证现存语料 `*` 仅作字段键、`-` 仅作内容列表项，双层约定稳定无混用——A4 只需显式钉死该两级约定，无需额外归一化
  - Phase 5 预检新增「**可解析性检查**」（渲染前断言每页字段完整还原，缺失即停）
  - 验收：`sample01`（中文 deck）+ `sample02`（英文 deck）**双语料**字段全量解析通过，无丢失、无误切分
  - **语料覆盖缺口（review-01 §5-3 修正）**：样本 deck 仅覆盖 **5/6** archetype（`SPLIT_50_50` 使用 2 次，`PROCESS_TIMELINE` 从未使用）——需补 1 个 synthetic `PROCESS_TIMELINE` fixture 页（按 design.md registry 定义构造）或明示接受 5/6 覆盖验收

### B. 渲染引擎层（可复用 + 确定性）

- [ ] **B1. 新建顶层 `10-tools/` 目录**
  - 渲染引擎放 `10-tools/render/`，与输出/素材解耦
  - 预留未来工具位（缩略图、QA、批量等），本次仅建 `render/`
  - AGENTS.md 同步补充 `10-tools/` 约定（工具代码，非中间产物）

- [ ] **B2. 几何寄存器数据化**
  - 把 6 个 archetype 的坐标/尺寸/字号从函数体抽出为 JSON 数据（`ARCHETYPE_REGISTRY`，命名与 archetype 词汇表统一）
  - renderer 改为查表纯函数 → 执行层对同一输入**内容级确定**（决策依据：review-7th §1.1 实证——python-pptx 将墙钟时间写入 zip 元数据，文件级字节必然不同，内容级 XML 一致）
  - 验收：同一输入重跑 2 次，解包后各 XML part 内容哈希一致（忽略 zip entry mtime；可选加固：构建后重写 zip 元数据固定时间戳，实现字节级一致）

- [ ] **B3a. `build_deck.py` 薄壳化（S1 阶段）**
  - `06-outputs/build_deck.py` 从 ~350 行缩减到 ~40-60 行：读本工作区独立的 `slides_data.json`（由现 build_deck.py 数据层抽离）→ 调 `10-tools/render` 的 `build()`
  - **契约边界：S1 数据源 = `slides_data.json`，不得依赖 S2 的 design frontmatter 或 S3 的 deck-content 解析**
  - 验收：渲染结果与 V1.0 视觉等价

- [ ] **B3b. 数据源切换为上游规范（S3 阶段）**
  - `slides_data.json` 替换为「design.md frontmatter（S2）+ deck-content 解析（A4/S3）」驱动的编译
  - 验收：改一行 deck 文案 → 重跑后两端输出自动同步更新，无人工二次编辑

- [ ] **B4. Marp 资产对称抽取**
  - CSS（archetype 的 Marp 译文）抽到 `10-tools/render/marp/marp-theme.css`，经 `--theme` 注入
  - `marp_deck.md` 由编译器生成（可从 deck-content 自动拼装）
  - 验收：HTML 输出与 V1.0 视觉等价，CSS 不再长文内联在 md 中

### C. 校验与自愈层（对齐可验证）

- [ ] **C1. Phase 5 预检升级**
  - 三源一致性 + Layout Tag 词表 + 几何字段完备性全部机械化（基于 A2 的 frontmatter 数据）
  - `10-tools/render/schema/design.schema.json` 提供 schema

- [ ] **C2. Post-build 一致性校验**
  - 渲染后反向提取：PPTX 文本、HTML `<section>` 文本 → 与 `deck-content.md` 机械化 diff
  - HTML slide 计数断言（V1.0 已有，纳入统一校验模块）

- [ ] **C3. 渲染产物管理**
  - `build_deck.py` / `marp_deck.md` 定位为「编译产物」，不再手工维护
  - 渲染失败时自愈循环沿用 V1.0 机制（依赖安装、代码修复、重跑）

### D. 流程与纪律

- [ ] **D1. 无内联纪律**：引擎代码永不内联任何 deck 内容（数据全部参数化进入）——真复用 vs 假复用的验收线
- [ ] **D2. 新 archetype 扩展 SOP**：改几何寄存器 + 1 个 renderer + 1 个 CSS class，三处定点修改，不动全局
- [ ] **D3. HITL 关卡不变**：Phase 2 结构确认 / Phase 4 人面审阅保持

---

## 二、实施阶段（含独立验收）

| 阶段 | 内容 | 验收标准 |
| :--- | :--- | :--- |
| **S1** | **Step-0 工作集恢复（review-01 §4-1）**：`git show ea71cab:06-outputs/{build_deck.py,deck-content.md,design.md,info.md,outline.md}` + `git show c46fc87:06-outputs/marp_deck.md` 恢复工作区，单独 commit 作 S1 基线锚点（06-outputs 现为空，引擎只存于 git 历史）→ 建 `10-tools/render/`，重构现有渲染引擎（B2 几何寄存器 JSON 化）+ 薄壳化 build_deck.py（**B3a**，数据源=独立 `slides_data.json`）+ 落 `10-tools/render/verify.py`（part 级内容哈希对比，review-01 §4-3：兼作 B2 确定性 / S1 golden / C2 种子） | 用恢复出的 deck 重跑，执行层内容级确定性验证（同输入重跑 2 次解包后 XML part 全一致）；golden=`ea71cab`/`c46fc87` git blob 作**回归参考**比对（详见 §四 待确认决策）；最终视觉由人审阅 |
| **S2** | design.md 双层化（A2）+ schema.json（C1 前置） | 预检能机械化校验 frontmatter 数据；Phase 4 审阅体验不回退 |
| **S3** | **测试语料落位（review-01 §4-2）**：将双语料复制为 `10-tools/render/tests/fixtures/` 下受控、受跟踪文件（`_samples/` 为排除目录且 gitignored，不得作为永久测试依赖）；人工确认 sample01 语料语言 + 补 `PROCESS_TIMELINE` synthetic fixture → 内容去内联（A1）+ deck-content 解析规格落地（**A4**）+ 数据源切换（**B3b**）+ Marp 资产抽取（B4）+ post-build diff 校验（C2） | 改一行 deck 文案 → 两端同步更新，无人工二次编辑；双语料+ PROCESS_TIMELINE fixture 解析通过；diff 校验通过 |
| **S4** | AGENTS.md 契约更新（10-tools 说明、新 Phase 5 流程、扩展 SOP、D 系列纪律）+ 文档 + git 提交 | 全链重跑一次 + 文档评审 |

---

## 三、风险与回滚

- **风险：** b2 几何数据化可能微调坐标 → 视觉略变。缓解：以 git blob `ea71cab`/`c46fc87` 为 golden **回归参考**比对（勿依赖 `_samples/` 副本）；「视觉等价」仅约束 S1 纯重构范围，非长期锁死——超出范围的设计变更走 Phase 4 HITL 人审后更新基准。
- **风险：** 去内联化涉及解析 deck-content.md 的散文式正文（口播稿含中文引号/冒号/换行），解析边界可能误切分。缓解：**A4 解析规格先行定义安全定界（YAML 块/固定标记）+ per-archetype 字段 schema + 预检可解析性检查 + 双语料（sample01 中文 / sample02 英文）验收后再实施 S3**——不依赖 S2 的 design.schema.json（其只覆盖 design frontmatter，管不了 deck-content 正文）。
- **回滚：** 每阶段独立 commit，可回退至上一里程碑（S1 含 Step-0 独立基线锚点 commit）。

---

## 四、待确认决策

> review-7th §3.4 已实证裁决以下三项，标注后解除待定状态：

- [x] `10-tools/` 命名：✅ 采纳（数字前缀与 00-06 阶段目录排序兼容；`_` 前缀保留给排除目录，语义分层清晰）
- [x] 实施顺序：✅ **S1→S2→S3→S4 全量按序**（否决"仅 S1+S3"——S3 解析依赖 S2 schema 前置，且 B3a/B3b 拆分后 S1 不再越过 S2）
- [x] Golden 基准：✅ 定位为**参考对照基线（approved baseline）而非最终审批标准**（review-02）。具体语义：① V2.0 输出的 PPTX/HTML **最终由人判断与给意见**，不依赖 `_samples/` 下例子的 html/presentation 作判定；② golden 仅作 S1/S2 回归参考——走 git blob（PPTX=`git show ea71cab:06-outputs/presentation.pptx`，HTML=`git show c46fc87:06-outputs/presentation.html`），勿依赖 `_samples/sample02` 副本（gitignore 区存在被无声篡改风险）；③ 设计与内容差异检测统一走**程序化 diff**（结构/文本/颜色/slide-count，见 C2 + verify.py），不要求 AI 逐页全量审阅历史产物；④ 每次设计经 Phase 4 HITL 人审批准后，可将新输出更新为新基线——允许基线演进，不绑死历史产物。

> 以下为 review-01/02 核实后新增裁决（review-8th）：
- [x] S1 Step-0 工作集恢复：✅ 采纳（06-outputs 现仅 `.gitkeep`，`build_deck.py` 只存在于 git 历史——已实证）
- [x] A4 需 per-archetype on-slide 字段 schema：✅ 采纳（实证 6 个 render 函数签名字段形状各异）
- [x] 列表符混用假设：❌ 否决（实证 `*`=字段键 / `-`=列表项，双层约定稳定，A4 仅需文档化）
- [x] 语料覆盖充分性：❌ 修正——样本 deck 仅覆盖 **5/6** archetype（`PROCESS_TIMELINE` 未使用），S3 需补 synthetic fixture 或明示接受 5/6
- [x] 测试语料落位 fixtures：✅ 采纳（`_samples/` gitignored + 排除目录，A4 验收不可复现，需复制为受控受跟踪文件）